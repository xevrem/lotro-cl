{
  "name": "lotro-cl",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "idb": "^2.1.0",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-modal": "^3.3.2",
    "react-scripts": "1.1.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "sass": "node-sass-chokidar src/ -o src/ --watch --recursive"
  },
  "devDependencies": {
    "node-sass-chokidar": "^0.0.3"
  }
}
